# Ask for user first & last name
    # Input validation
# Ask for user's division
    # Present options
        # Input validation

#Import-Module ActiveDirectory
$first_name = Read-Host -Prompt "Enter the user's first name"
$last_name = Read-Host "Enter the user's last name"
Write-Host "You wrote $first_name $last_name.`nIs that correct?"